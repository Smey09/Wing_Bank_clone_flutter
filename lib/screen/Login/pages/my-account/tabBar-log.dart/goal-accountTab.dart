import 'package:flutter/material.dart';

class GoalAccountTab extends StatelessWidget {
  const GoalAccountTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Goal content page"),
    );
  }
}
